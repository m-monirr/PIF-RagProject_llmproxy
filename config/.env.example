# ============================================================================
# PIF RAG CHAT - ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy this file to '.env' and fill in your actual values
# Command: cp .env.example .env (Mac/Linux) or copy .env.example .env (Windows)
# ============================================================================

# ----------------------------------------------------------------------------
# GROQ API CONFIGURATION (PRIMARY LLM - FREE TIER)
# ----------------------------------------------------------------------------
# Get your FREE API key: https://console.groq.com/keys
# Model: llama-3.1-8b-instant (FAST & FREE!)
# Rate Limit: 30 requests/minute (free tier)
GROQ_API_KEY=gsk_your_actual_groq_api_key_here

# ----------------------------------------------------------------------------
# OLLAMA CONFIGURATION (LOCAL EMBEDDINGS)
# ----------------------------------------------------------------------------
# For LOCAL Ollama (recommended - runs on your machine):
OLLAMA_BASE_URL=http://localhost:11434

# Model for embeddings (must be pulled first: ollama pull qwen3-embedding)
EMBED_MODEL_ID=qwen3-embedding
EMBED_DIMENSION=4096

# For Ollama Cloud (alternative - requires account):
# OLLAMA_BASE_URL=https://api.ollama.ai
# OLLAMA_API_KEY=your_ollama_cloud_api_key_here

# ----------------------------------------------------------------------------
# QDRANT VECTOR DATABASE
# ----------------------------------------------------------------------------
# Local Docker container (default):
QDRANT_HOST=localhost
QDRANT_PORT=6333

# For Qdrant Cloud (alternative):
# QDRANT_HOST=your-cluster.aws.cloud.qdrant.io
# QDRANT_API_KEY=your_qdrant_cloud_key

# ----------------------------------------------------------------------------
# OPTIONAL: LANGFUSE (LLM MONITORING & OBSERVABILITY)
# ----------------------------------------------------------------------------
# Get keys from: https://cloud.langfuse.com
# Uncomment to enable:
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_HOST=https://cloud.langfuse.com

# ----------------------------------------------------------------------------
# OPTIONAL: OPENAI (ALTERNATIVE LLM PROVIDER)
# ----------------------------------------------------------------------------
# Only needed if you want to use OpenAI models
# OPENAI_API_KEY=sk-...

# ----------------------------------------------------------------------------
# OPTIONAL: COHERE (ALTERNATIVE LLM PROVIDER)
# ----------------------------------------------------------------------------
# Only needed if you want to use Cohere models
# COHERE_API_KEY=your-cohere-api-key

# ============================================================================
# NOTES:
# ============================================================================
# 1. GROQ_API_KEY is REQUIRED (free tier available)
# 2. Ollama must be installed locally for embeddings
# 3. Qdrant runs in Docker (included in setup)
# 4. LangFuse is optional but recommended for production monitoring
# 5. Never commit this file to version control (.env is in .gitignore)
# ============================================================================